@page "/chat"

@using Microsoft.Fast.Components.FluentUI

<style>
    /*body{
        justify-content: center;
        align-items: center;
        height: 100vh;
        width: 100vw;
        background-color: transparent;
    }

    .chat {
        display: flex;
        flex-direction: column;
        justify-content: flex-end;
        height: 400px;
        overflow: scroll;
        width: 200px;
        background-color: #ffffff;

    }

    .chat-bubble {
        background-color: #f1f1f1;
        border-radius: 5px;
        padding: 10px;
        margin: 10px 0;
        max-width: 80%;
        width: fit-content;
        color: black;
        float: right;
        text-align: right;
        right: 0;
    }*/

body{
	background: #EEEEEE;
	font-family: 'Roboto', sans-serif;
    justify-content: center;
    align-items: center;

}

.card{
	width: 300px;
	border: none;
	border-radius: 15px;
    height: 400px;
    overflow: scroll;
    justify-content: flex-end;
}
.adiv{
	background: #84bb8d;
	border-radius: 15px;
	border-bottom-right-radius: 0;
	border-bottom-left-radius: 0;
	font-size: 12px;
	height: 46px;
}
.chat{
	border: none;
	background: #E2FFE8;
	font-size: 10px;
	border-radius: 20px;
}
.bg-white{
	border: 1px solid #E7E7E9;
	font-size: 10px;
	border-radius: 20px;
}

.dot{
	font-weight: bold;
}
.form-control{
	border-radius: 12px;
	border: 1px solid #F0F0F0;
	font-size: 8px;
}
.form-control:focus{
	box-shadow: none;
	}
.form-control::placeholder{
	font-size: 8px;
	color: #C4C4C4;
}

</style>


    @* <div class="chat">
        @foreach (var item in messages){
            <div class="chat-bubble">
                @item
            </div>
        }
    </div>
    <FluentTextField @bind-Value=value3 style="width:200px;"></FluentTextField>
    <FluentButton @onclick="AddMessage">Add</FluentButton>


 *@

<div class="container d-flex justify-content-center">
  <div class="card mt-5">
    @* <div class="d-flex flex-row justify-content-between p-3 adiv text-white">
      <i class="fas fa-chevron-left"></i>
      <span class="pb-3">Live chat</span>
      <i class="fas fa-times"></i>
    </div> *@
    @* On left *@
      @* <div class="d-flex flex-row p-3">
        <img src="https://img.icons8.com/color/48/000000/circled-user-female-skin-type-7.png" width="30" height="30">
        <div class="chat ml-2 p-3">Hello and thankyou for visiting birdlymind. Please click the video above</div>
      </div> *@

    @foreach (var item in messages){
        <div class="d-flex flex-row p-3">
            <div class="bg-white mr-2 p-3"><span class="text-muted" style="float:right; right:0;">@item</span></div>
            <img src="https://img.icons8.com/color/48/000000/circled-user-male-skin-type-7.png" width="30" height="30">
        </div>
    }
      
      
    <div class="form-group px-3">
        <FluentTextField @bind-Value=value3 style="width:200px;"></FluentTextField>
        <FluentButton @onclick="AddMessage">Add</FluentButton>
    </div>
  </div>
</div>






@code {
    string? value1, value2, value3, value4;
    public List<string> messages = new List<string>();

    public void AddMessage()
    {
        Console.WriteLine(value3);
        messages.Add(value3);
        
        foreach (var item in messages)
        {
            Console.WriteLine(item);
        }
        value3 = "";
        StateHasChanged();
    }


}